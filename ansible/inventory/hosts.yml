---
# Ansible inventory for dotfiles automation
# This is a simple localhost-only setup

all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: /usr/bin/python3

  vars:
    # Global variables (can be overridden in group_vars)
    ansible_become: true
    ansible_become_method: sudo
